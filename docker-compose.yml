services:
  db:
    image: postgres:17
    environment:
      POSTGRES_DB: bazi
      POSTGRES_USER: bazi
      POSTGRES_PASSWORD: bazi
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
